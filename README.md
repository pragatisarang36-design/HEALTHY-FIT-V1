# Healthy Fit

Fitness app by Pragati Sarang.

Standalone Vite + React fitness tracker.

## Local Development

Install dependencies:

```bash
npm install
```

Create `.env.local` from `.env.local.example` and set your environment values.

Run the app:

```bash
npm run dev
```
